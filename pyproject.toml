[project]
name = "lightning-workspace"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "comet-ml",
    "lightning",
    "rich",
    "wandb",
]

[tool.uv.sources]
# Resolve ``lightning`` from the sibling upstream checkout, not PyPI.
lightning = { path = "../lightning", editable = true }
